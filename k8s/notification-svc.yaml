# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: api-gateway
#   labels:
#     app: api-gateway
# spec:
#   replicas: 2
#   selector:
#     matchLabels:
#       app: api-gateway
#   template:
#     metadata:
#       labels:
#         app: api-gateway
#     spec:
#       containers:
#         - name: api-gateway
#           image: aparnasukesh/api-gateway:latest
#           ports:
#             - containerPort: 8080
#           resources:
#             requests:
#               memory: "128Mi"
#               cpu: "250m"
#             limits:
#               memory: "256Mi"
#               cpu: "500m"
#           livenessProbe:
#             httpGet:
#               path: /health
#               port: 8080
#             initialDelaySeconds: 5
#             periodSeconds: 10
#           readinessProbe:
#             httpGet:
#               path: /ready
#               port: 8080
#             initialDelaySeconds: 5
#             periodSeconds: 10
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: api-gateway
#   labels:
#     app: api-gateway
# spec:
#   type: LoadBalancer
#   ports:
#     - port: 8080
#       targetPort: 8080
#   selector:
#     app: api-gateway


apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-svc
  labels:
    app: notification-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-svc
  template:
    metadata:
      labels:
        app: notification-svc
    spec:
      containers:
        - name: notification-svc
          image: aparnasukesh/notification-svc:latest
          ports:
            - containerPort: 5051
          resources:
            requests:
              memory: "128Mi"
              cpu: "250m"
            limits:
              memory: "256Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: notification-svc
  labels:
    app: notification-svc
spec:
  type: LoadBalancer
  ports:
    - port: 5051
      targetPort: 5051
  selector:
    app: notification-svc
